import { Accordion } from "@mui/material";
import React from "react";

const accStyle = {
  height: "100px",
};

const AccordionCustom = () => {
  return <Accordion sx={accStyle}></Accordion>;
};

export default AccordionCustom;
